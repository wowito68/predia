# Entorno virtual (1.2G) — recrear con: python3 -m venv .venv && .venv/bin/pip install -r requirements.txt
.venv/
__pycache__/
*.pyc
*.pyo
.ipynb_checkpoints/

# Artefactos binarios grandes y REPRODUCIBLES (regenerar con run_train.py):
#   datasets/ (splits ~41M) y models/ (~61M). El modelo final está en exports/.
datasets/
models/
